/**
 * Instructions:- Create a variable named `hasAccess` and
 * set it to `true`. Then write an if statement that
 * checks if `hasAccess` is true. If it is, log the message
 * 'Access granted' to the console.
 */

// solution

// Create the hasAccess variable
let hasAccess = true;
// Write your if statement below
if (hasAccess) {
  console.log("Access granted");
}
