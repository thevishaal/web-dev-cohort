/**
 * Create an arrow function `double` that takes a number and returns it doubled, using implicit return (no braces, no return keyword). Call it with 7 and log the result.
 */

// solution
// Create arrow function with implicit return
const double = (n) => n * 2;
// Call and log
console.log(double(7));
