/**
 * Create an arrow function `square` that takes a number `n` and returns `n * n`. Call it with 5 and log the result.
 */

// solution

// Create arrow function
const square = (n) => {
  return n * n;
};
// Call and log
console.log(square(5));
