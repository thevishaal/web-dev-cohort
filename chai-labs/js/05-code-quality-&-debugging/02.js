/**
 * Create three variables: `name` = 'Alice', `age` = 25, and `city` = 'Paris'. Log each variable with a descriptive label using template literals. For example: 'Name: Alice'
 */

// solution

// Create your variables
const name = "Alice";
const age = 25;
const city = "Paris";
// Log each with descriptive labels
console.log(`Name: ${name}`);
console.log(`Age: ${age}`);
console.log(`City: ${city}`);
