/**
 * Create an object named `user` with three properties: `name` set to 'Alice', `age` set to 25, and `isActive` set to true. Log the entire object to the console.
 */

// solution

// Create your user object
const user = {
  name: "Alice",
  age: 25,
  isActive: true,
};
// Log the object
console.log(user);
