/**
 * Create a Set named `numbers` with values 1, 2, 3, 4, 5 using the Set constructor and an array. Log the set to see its contents.
 */

// solution

// Create your Set
const numbers = new Set([1, 2, 3, 4, 5]);
// Log the set
console.log(numbers);
