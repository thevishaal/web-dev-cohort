/**
 * Create an empty Set named `fruits`. Use add() to add 'apple', then 'banana', then 'apple' again. Log the Set to see that 'apple' appears only once.
 */

// solution

// Create an empty Set
const fruits = new Set();
// Add fruits
fruits.add("apple");
fruits.add("banana");
fruits.add("apple");
// Log the set
console.log(fruits);
