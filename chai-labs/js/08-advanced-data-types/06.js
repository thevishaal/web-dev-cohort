/**
 * Create a Map named `ages` with initial entries: 'Alice' -> 25, 'Bob' -> 30. Use the Map constructor with an array of [key, value] pairs. Log the map.
 */

// solution

// Create your Map
const ages = new Map([
  ["Alice", 25],
  ["Bob", 30],
]);
// Log the map
console.log(ages);
