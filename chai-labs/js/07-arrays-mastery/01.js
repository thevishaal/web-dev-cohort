/**
 * Create an array named `colors` with three string elements: 'red', 'green', and 'blue'. Then log the array to see its contents.
 */

// solution

// Create your colors array
const colors = ["red", "green", "blue"];
// Log the array
console.log(colors);
