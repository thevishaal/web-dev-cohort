/**
 * Create an array named `numbers` with five numbers: 10, 20, 30, 40, 50. Then create a variable `count` that stores the length of the array. Log both the array and the count.
 */

// solution

// Create your numbers array
const numbers = [10, 20, 30, 40, 50];
// Get the length
const count = numbers.length;
// Log both values
console.log(numbers, count);
