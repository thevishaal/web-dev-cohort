/**
 * Create an array named `fruits` with ['apple', 'banana', 'cherry', 'date']. Access the first element (index 0) and store it in a variable `first`. Access the third element (index 2) and store it in `third`. Log both variables.
 */

// solution

// Create your fruits array
const fruits = ["apple", "banana", "cherry", "date"];
// Access first and third elements
const first = fruits[0];
const third = fruits[2];
// Log the values
console.log(first, third);
