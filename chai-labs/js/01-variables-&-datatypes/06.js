/**
 * Create a variable named `firstName` with value 'John' and `lastName` with value 'Doe'. Then create a `fullName` variable that combines them with a space in between.
 */

// solution

// Declare firstName, lastName, and fullName
const firstName = "John";
const lastName = "Doe";
const fullName = firstName + " " + lastName;
