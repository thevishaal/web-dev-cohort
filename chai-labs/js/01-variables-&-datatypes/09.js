/**
 * Declare three variables in separate statements: `x` with value 10, `y` with value 20, and `z` with value 30. Then log the sum of x + y + z to the console.
 */

// solution

// Declare three variables
let x = 10;
let y = 20;
let z = 30;
console.log(x + y + z);
